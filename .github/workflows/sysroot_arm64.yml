name: Sysroot (arm64)

on:
  push:
    paths:
      - 'sysroot/**'
      - '!sysroot/*.md'
      - '!sysroot/configs/**'
      - 'sysroot/configs/sysroot_arm64_defconfig'
  workflow_dispatch:

jobs:

  build:
    name: Build (arm64)
    uses: ./.github/workflows/sysroot.yml
    with:
      arch: arm64
