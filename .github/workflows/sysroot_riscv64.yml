name: Sysroot (riscv64)

on:
  push:
    paths:
      - 'sysroot/**'
      - '!sysroot/*.md'
      - '!sysroot/configs/**'
      - 'sysroot/configs/sysroot_riscv64_defconfig'
  workflow_dispatch:

jobs:

  build:
    name: Build (riscv64)
    uses: ./.github/workflows/sysroot.yml
    with:
      arch: riscv64
